# 新文件: ARCHITECTURE.md

# 项目架构说明

## 设计理念

本项目采用**分层架构**和**模块化设计**，遵循以下原则：

1. **单一职责**：每个模块只负责一个功能
2. **依赖倒置**：高层模块不依赖低层模块的具体实现
3. **配置外置**：所有配置通过环境变量管理
4. **可选依赖**：非核心功能（如 OpenAI）采用延迟导入

## 核心模块

### 1. DocumentProcessor（文档处理器）
- **职责**：加载和分割文档
- **支持格式**：PDF, DOCX, TXT
- **关键特性**：
  - 自动检测文件编码
  - 支持自定义分割参数
  - 错误隔离（单个文件失败不影响其他）

### 2. VectorStore（向量存储）
- **职责**：管理向量数据库
- **支持后端**：Chroma, FAISS
- **支持 Embedding**：DashScope, OpenAI, BGE
- **关键特性**：
  - 工厂模式创建 embeddings
  - 自动持久化
  - 支持清空重建

### 3. LLMEngine（大语言模型引擎）
- **职责**：封装 LLM 调用
- **支持模型**：通义千问, OpenAI GPT
- **关键特性**：
  - 根据模型名自动选择提供商
  - 支持温度参数调整
  - 成本控制在 max_tokens

### 4. RAGChain（RAG 问答链）
- **职责**：组装完整的 RAG 流程
- **支持策略**：stuff, map_reduce, refine
- **关键特性**：
  - 可定制的 prompt 模板
  - 格式化响应结果
  - 返回来源文档

### 5. QueryCache（查询缓存）
- **职责**：缓存查询结果，降低 API 成本
- **实现**：内存字典 + MD5 哈希键
- **关键特性**：
  - 自动标准化查询（忽略大小写和空格）
  - 可启用/禁用
  - 统计缓存命中率

### 6. PerformanceMonitor（性能监控）
- **职责**：跟踪系统性能指标
- **监控项**：响应时间、查询次数、缓存命中率
- **关键特性**：
  - 装饰器模式计时
  - 实时统计
  - 可重置指标

## 数据流

用户问题
→ Cache 检查
→ VectorStore 检索相关文档
→ RAGChain 组装 Prompt
→ LLMEngine 调用大模型
→ 返回答案并缓存
→ Monitor 记录性能